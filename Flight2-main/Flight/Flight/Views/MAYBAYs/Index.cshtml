@model IEnumerable<Flight.Models.MAYBAY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoaiMayBay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiMayBay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaMayBay }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaMayBay }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaMayBay })
        </td>
    </tr>
}

</table>
