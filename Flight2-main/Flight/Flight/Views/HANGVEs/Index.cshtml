@model IEnumerable<Flight.Models.HANGVE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHangVe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHangVe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHangVe }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHangVe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHangVe })
        </td>
    </tr>
}

</table>
